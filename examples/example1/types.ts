export type UserType = {
    name: string;
    surname: string;
    email: string;
    birthYear: Date;
}

export interface IUser {
    name: string;
    surname: string;
    email: string;
    birthYear: Date;
}